SELECT D.DEPT_NAME FROM DEPARTMENTS D WHERE D.DEPARTMENT_ID = (SELECT DEPARTMENT_ID FROM EMPLOYEES GROUP BY DEPARTMENT_ID ORDER BY AVG(AGE) DESC FETCH FIRST 1 ROW ONLY);

DEPT_NAME
--------------------------------------------------
カスタマーサポート部